class Solution {
public:
    int dfs(vector<int>& jobDifficulty, int d, int i, int cur) { 
        int n = jobDifficulty.size();
        if (i == n and d == 0) {
            return cur;
        }
        if (d <= 0) {
            return -1;
        }
        
    }

    int minDifficulty(vector<int>& jobDifficulty, int d) { 

    }
;
